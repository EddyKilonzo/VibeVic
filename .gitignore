# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
dist/
build/

# Environment. Every one of them, at every depth — the repo holds a Nest
# server and a Next app, each with its own, and none of them is ever
# committed. No template is checked in either; see backend/.gitignore.
.env
.env.*
**/.env
**/.env.*

# TypeScript / Next
*.tsbuildinfo
next-env.d.ts

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/

# Test / coverage
coverage/

.vercel
